<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>New Databox Submission</title>
  <style>
    body {
      font-family: sans-serif;
      line-height: 1.6;
      color: #333;
      background-color: #f8f9fa;
      padding: 20px;
    }
    .container {
      max-width: 600px;
      margin: auto;
      background: #ffffff;
      border: 1px solid #e2e8f0;
      border-radius: 8px;
      padding: 24px;
    }
    .header {
      font-size: 20px;
      margin-bottom: 16px;
      font-weight: bold;
      color: #1a202c;
    }
    .section-title {
      margin-top: 20px;
      font-weight: 600;
      color: #2d3748;
    }
    .file-list {
      margin: 0;
      padding-left: 20px;
    }
    .file-list li {
      margin-bottom: 4px;
    }
    .footer {
      margin-top: 32px;
      font-size: 12px;
      color: #718096;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">📦 New Databox Submission</div>

    <div class="section-title">Message:</div>
    <p>{{Message}}</p>

    <div class="section-title">From:</div>
    <p>{{From}}</p>

    <div class="section-title">Files:</div>
    <ul class="file-list">
      {{#each Files}}
        <li>{{this.FileName}}</li>
      {{/each}}
    </ul>

    <div class="footer">
      Sent by Databox • {{Date}}
    </div>
  </div>
</body>
</html>
``
